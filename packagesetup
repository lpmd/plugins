#
#
#

name = plugins
fullname = 'Plugins Pack'
version = 0.7.0
author = 'GNM <gnm@gnm.cl>'
prefix = /usr/local
mainfirst = true

language c++

   ifdef intel
    compiler = mpiCC
    linker = mpiCC
    flags = -Wall -O3 -axSSE4.1 -parallel -diag-disable remark -finline-functions -DNDEBUG  -fPIC -I.
    ifdef openmp
     openmpflags = -openmp
    endif
   else
    compiler = mpiCC
    linker = mpiCC -fPIC
    flags = -g -Wall -O3 -ffast-math -funroll-all-loops -finline-functions -fPIC -std=gnu++98 -I.
    ifdef openmp
     openmpflags = -fopenmp
     endif
   endif

end

language c

   ifdef intel
    compiler = icc
   else
    compiler = gcc
   endif

   ifdef intel 
    flags = -Wall -O3 -std=c99 -fast -diag-disable remark -DNDEBUG -fPIC
   else 
    flags = -g -Wall -O3 -ffast-math -finline-functions -fPIC -std=c99
   endif

   ifdef intel
    linker = icc
   else
    linker = gcc
   endif

end


language plugcode
ifdef intel
   flags = -Wall -O3 -std=c99 -fast -diag-disable remark -DNDEBUG -fPIC
   linker = icc
else
   flags = -g -Wall -O3 -ffast-math -finline-functions -std=gnu99
   linker = gcc
endif
end

directory .

    module test.so
       language = plugcode
       sources = test.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module buckingham.so
       language = plugcode
       sources = buckingham.plugcode
       instsallpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module centeratoms.so
       language = plugcode
       sources = centeratoms.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

#    module crystal.so
#       language = plugcode
#       sources = crystal.plugcode
#       installpath = lib/lpmd
#       pkgconfig += liblpmd-3.0
#       version = 1.0.0
#    end

    module displace.so
       language = plugcode
       sources = displace.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module fcc.so
       language = plugcode
       sources = fcc.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module index.so
       language = plugcode
       sources = index.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module lennardjones.so
       language = plugcode
       sources = lennardjones.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module lennardjonesMod.so
       language = plugcode
       sources = lennardjonesMod.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module linearmomentum.so
       language = plugcode
       sources = linearmomentum.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module linkedcell.so
       language = plugcode
       sources = linkedcell.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module mcy.so
       language = plugcode
       sources = mcy.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module minimumimage.so
       language = plugcode
       sources = minimumimage.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module randomconfig.so
       language = plugcode
       sources = randomconfig.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module settag.so
       language = plugcode
       sources = settag.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module temperature.so
       language = plugcode
       sources = temperature.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module velocityverlet.so
       language = plugcode
       sources = velocityverlet.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module verlet.so
       language = plugcode
       sources = verlet.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module xyz.so
       language = plugcode
       sources = xyz.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

    module zeroforces.so
       language = plugcode
       sources = zeroforces.plugcode
       installpath = lib/lpmd
       pkgconfig += liblpmd-3.0
       version = 1.0.0
    end

end
